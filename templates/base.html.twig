<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
      {% block stylesheets %}
<link rel="stylesheet" href="{{ asset('build/bootstrap.min.css') }}">
<link rel="stylesheet" href="{{ asset('build/app.css') }}">
<link rel="stylesheet" href="{{ asset('build/font-awesome.css') }}">

    {% endblock %}
    </head>
    <body>
    <nav class="navbar navbar-expand-lg navbar-dark btn btn-danger">
      <a class="navbar-brand" href="{{path('home')}}">recette de jour</a>
      <span>Contacter nous: <i class="fa fa-phone" aria-hidden="true"></i> +216 27 589 590</span>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item active">
            <a class="nav-link" href="{{path('app_recettes_show') }}">Acheter</a>
          </li>

        </ul>
        <div class="form-inline my-2 my-lg-0">
          <ul class="navbar-nav">
        <li class="nav-item">
                <a class="nav-link mr-sm-2" href="#">connexion</a>
              </li>
              <li class="nav-item">
                      <a class="nav-link my-2 my-sm-0" href="#">inscription</a>
                </li>
                </ul>
    </div>
      </div>
    </nav>
    
        {% block body %}


      {% endblock %}
        {% block javascripts %}

<link rel="stylesheet" href="{{ asset('build/bootstrap.min.js') }}">

      {% endblock %}
    </body>
</html>
